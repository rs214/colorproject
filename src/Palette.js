import React, {Component} from 'react';

export default class Palette extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
      Palette
      </div>
    );
  }
}
